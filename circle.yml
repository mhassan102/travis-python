#general:
#  build_dir: ios-app

machine:

  xcode:
    version: 8.2

dependencies:
  override:
    - pip install tox tox-pyenv
    - pyenv local 2.7.10 3.5.1

test:
  override:
    - lsb_release -a
    - echo $(python --version)
    - ls
    - pwd
    - pyenv
