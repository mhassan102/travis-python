#general:
#  build_dir: ios-app

machine:

  xcode:
    version: 8.2

  post:
    - pyenv global 2.7.12 3.4.4

  #python:
  #  version: 3.5.1

  #python:
  #  version: 3.5.2



test:
  override:
    - lsb_release -a
    - echo $(python --version)
    - ls
    - pwd

